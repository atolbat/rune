kernel: Task 167 — THE SYNC-POINT PASS (the audit answer to "keep going": a live CDP profile of the GPU-Embers TF tier found 84.6% of ALL sampled CPU time blocked inside ONE call — the Task-69 error drain's getError at the end of step(); everything the Tasks-143..165 passes had been shaving was noise next to it) — the fix landed in @rune/gl, @rune/webgl2 and @rune/webgpu:

(1) THE PROBE CADENCE (webgl2Renderer.ts drainGlErrors): getError is not a passive flag peek — the driver (and ANGLE ahead of it) must complete every already-submitted command before it can answer, so the once-per-frame drain was a forced FULL PIPELINE FLUSH in the middle of the frame loop (the WebGL best-practices anti-pattern; the TF tier's ~170 transform-feedback passes all executed inside that single wait). The drain now runs as a health PROBE every 8th frame: the GL error flag is STICKY (an error sits set until getError retrieves it, new errors are DISCARDED while one pends — GLES semantics), so the probe still surfaces the FIRST error of its window; only the exact code of a second, DIFFERENT error within the same window can coalesce away. HUNTING MODE: when a probe finds an error the cadence snaps back to EVERY FRAME until a clean drain (the old diagnostic precision exactly when it matters — the spam guard keeps reports single-shot per code set); THE DISPOSE DRAIN: dispose() performs one final getError before WEBGL_lose_context, so a tail error raised after the last probe still surfaces at teardown (already-reported errors do not repeat); HEADLESS PARITY: an injected facade (rawContext === null) never touches getError, the historical contract unchanged. The honest measurement caveat, documented in code and docs: the container CANNOT demonstrate the speed delta cleanly — llvmpipe runs the TF tier so slowly that run-to-run variance (6-20x on byte-identical builds) swamps any A/B signal, and single-window frame counts there are lottery tickets; what carries the change is the mechanism (60 forced syncs per second for a diagnostic read → 7.5), the profile (the 84.6% blocked share, reproduced identically across two separate profile runs), and the sticky-flag semantics (detection value preserved). On real hardware the win is the standard one for removing redundant sync points: the submission path stops serializing against the GPU once per frame, the necessary sync stays where it belongs (the compositor's present).

(2) THE DRAWARRAYS MODE MAPPING (webgl2/realGL.ts): 'lines' / 'points' / 'triangle-strip' were silently drawn as TRIANGLES — the mode ternary's BOTH branches read gl.TRIANGLES (a copy-paste fossil) while the PrimitiveKind type promised all four; now mapped to LINES/POINTS/TRIANGLE_STRIP honestly. The executor only emits 'triangles' today, so nobody's pixels change — the facade becomes honest for the day a line/point soup is recorded. The WebGPU twin (realGPU.ts buildPipeline): desc.primitive maps to 'line-list' / 'point-list' alongside the existing 'triangle-strip' the same way.

TESTS: packages/gl/tests/task167.test.ts — 6 pins on a scriptable raw-context mock (getError called exactly once per 8 frames on a healthy page, zero on the injected-facade path; the sticky flag — an error raised in frame 1 surfaces at the frame-8 probe; hunting mode — an error snaps the drain to per-frame, the clean drain disarms it, the tick gate resumes, the getError-call arithmetic exact; the spam guard — a persistent storm reports once, a different code later reports anew; the dispose drain — a tail error surfaces at dispose with the dedicated wording, an already-probe-reported one does not) + packages/webgl2/tests/task167.test.ts — 5 pins (all four primitive modes reach their GL enums, instanced included).

Gates: 1751/1751 tests (was 1740, +11); typecheck 6 pre-existing (the particles/task142 baseline, untouched); lint 0 errors / 375 warnings (baseline); build → dist/rune.esm.js + min with the cadence; demo:smoke OK (vfx GPU health clean, mobile clean, astral alive: 72 systems / tap→panel / mobile 390); the NEW scripts/task167-syncpoint.mjs live gate PASS (GPU Embers on WebGL2 with the new dist: the loop advances, 25+ probe drains ran during the walk, zero page errors, the population in a sane band — the cadence arithmetic pinned exactly on the mock, the live gate pins survival); task164-steady both cells PASS (WebGPU Δ360/6s = 60fps class; GL alive and progressing); task152-keepalive PASS (the dispose drain did not disturb the park/resurrect contract).

Cache-busts ?v=167: rune.esm.js imports in demo/vfx/main.js, demo/vfx/demos/gpuEmbers.js, demo/astral/main.js; main.js?v=167 in demo/vfx/index.html; docs/particles-optimization.md gains the Task-167 dossier section.
