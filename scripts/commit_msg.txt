kernel: Task 164 — THE STEADY-STATE PASS (the follow-up audit: "продолжай оптимизировать ядро и вебгл/вебгпу пути" — the sort loop stops re-asserting its frame-static state, on BOTH backends) — the finding landed in @rune/webgpu and @rune/webgl2: the bitonic sort's hot loop re-asserts frame-static state ~342 times per frame at 160k particles (171 (bitonic, sortStep) pairs on WebGPU, ~348 TF passes on WebGL2), and every assert is machinery the frame already holds — the same shape the Task-163 audit found for GL texture binds, now hunted through both compute paths:

WEBGPU (realGPU.ts): (1) THE MERGED COMPUTE PASS — runCompute opened AND closed a GPUComputePassEncoder per call; consecutive dispatches now share ONE pass (opens on the first runCompute, closes at the frame's structural boundaries: a render pass opening, submit, the readTargetPixels copy — encoder-level ops are invalid under an open pass, and the copy lands after the dispatches so the readback observes post-compute state); dispatch ordering is a WebGPU guarantee (each dispatch its own sync scope, the implementation barriers read-after-write — the property the barrier-free API is built on), so merging cannot reorder effects: 342 begin/end pairs → 1; (2) THE UNIFORM WRITE-SKIP MEMO — every dispatch re-asserted the frame-static block via queue.writeBuffer (~342 identical queue ops per sorted frame); the per-family memo (a COPY of the last floats — the caller's scratch is reused) skips identical writes, NaN fields compare unequal and fall back to a real write, last-write-wins semantics preserved exactly: ~344 → ~2; (3) THE COMPUTE BIND-GROUP MEMO — setBindGroup(0, family.group) per dispatch → once per pass; (4) THE VERTEX-BIND MEMO (pass.setVertexBuffer with the slot's already-bound buffer skipped inside a render pass; the memo dies at every pass boundary — the GL twin of Task 163's unit-bind cache) + THE SAB STAGING CACHE (guardedWriteVertex copied SAB-backed feeds into a FRESH Uint8Array every frame — a ~MB-scale allocation per frame per feed, sustained GC churn for T1/T2 shared feeds; the staging is now cached per source view and grown on demand, only the copy remains);

WEBGL2 (realGL.ts): (5) THE PER-FIELD UNIFORM MEMO — the GL bitonic loop moves only (k, j) per pass but re-asserted the whole packed block (~6-8 uniform calls × 171); the per-record memo emits exactly the CHANGED fields (~2 per pass), an identical block emits nothing, a length change falls back to the full emit, the program is exclusively owned by its pass so the memo cannot go stale; (6) THE SAMPLER-UNIT MEMO — declaration slot i always samples unit i, uniform1i writes the UNIT INDEX not the texture, after the first run the value never changes (~178 redundant calls per sorted frame → 0; the Task-136 first-run contract pinned); (7) THE SCRATCH UPLOAD UNIT — texSubImage2DBuffer (the per-pass GPU→GPU state round-trip) bound TEXTURE_2D on the CURRENT unit and killed the WHOLE Task-163 unit-bind cache, so every follow-up bindTexture re-bound for real and the cache never survived a frame of sorting; the upload now binds on the LAST texture unit (probed via MAX_TEXTURE_IMAGE_UNITS, nobody's sampler lives there, the mirror entry for that unit alone is dropped, the feedback-loop ledger sees the real binding — more honest than the old invisible bind): the per-pass bindTexture becomes a cache hit, ~712 GL calls per sorted frame → ~0; plus the UNPACK_ALIGNMENT mirror (the PBO path's per-call pixelStorei(4) → once; the plain byte path's pin of 1 re-arms it); (8) THE HONEST NON-CHANGE: the eager restore discipline (VAO/TF/discard/buffer-base unbound after every pass) stays byte-identical — the pinned "the render executor never observes the TF family" contract is load-bearing, a lazy-restore scheme would save ~4 more calls per pass but puts a disarm check on every render-family entry;

TESTS: 20 new pins (task164webgpu.test.ts — the merged pass: one begin/end for N dispatches, close-before-render/submit ordering, burst-per-pass shape, the render-open guard, zero-workgroups contract; the uniform memo: content-based skip, mutated-scratch re-upload, per-family independence, clamp reporting; the bind-group memo; the vertex-bind memo with pass-boundary reset; the SAB staging identity + fresh bytes. task164.test.ts — the GL per-field memo: identical block, the bitonic shape, the length fallback, the A-B-A alternation; the sampler memo; the scratch upload unit: the cache survives the PBO round-trip, the whole-iteration steady-state profile; the alignment mirror);

VERIFIED: 1723 tests 0 fail, typecheck 6 pre-existing baseline (particles/task142, untouched), lint 0 err / 375 warn (baseline+1, the memo's non-null assertion — the codebase's pinned style), dist rebuilt; demo:smoke 24/24 vfx demos live (the carousel runs GPU Embers on the container's default WebGPU — the merged pass carried the full 160k tier at ~60 fps, GPU health clean); the NEW scripts/task164-steady.mjs gate PASS both backends (W: 60 fps, Δ360/6s, zero errors; G: alive + progressing + zero errors — the thresholds honest per backend: the container's llvmpipe TF emulation is brutally slow REGARDLESS of the library, the A/B proof: clean HEAD Δ3/6s → this build Δ8/6s, ~2.6× the frame rate with the population actually progressing instead of stalling; on real GPUs the TF tier runs at full rate per the Tasks 140-162 field logs); docs/particles-optimization.md — the Task 164 dossier; cache-busts: ?v=164 on the rune.esm.js imports in demo/vfx/main.js + demo/vfx/demos/gpuEmbers.js, main.js?v=164 in index.html
