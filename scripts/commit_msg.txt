kernel: Task 168 — THE RESTORE WIRE (the Task-137 comment carried a documented TODO: preventDefault kept the context RESTORABLE, and nothing ever picked the restore up — the journal machinery of Tasks 62-66 was built for this recovery but only as a MANUAL new-renderer flow; this pass wires the browser's own webglcontextrestored event into an IN-PLACE recovery) — three pieces landed:

(1) realGL.resetAfterContextRestore() — a new OPTIONAL method on GLFacade: after the loss+restore cycle the raw context is the SAME JS object but every GL object it ever handed out is dead, and the facade's Maps would keep pointing at corpses while the ids stay "known" — the exact silent-zombie shape (bindTexture finds the dead WebGLTexture, the GL ignores it, the canvas stays black while the loop counts frames). The reset returns the facade to its post-constructor state: programs/buffers/textures/targets/views/meta Maps cleared, id counters at zero, every Task-163/164/165 memo and mirror disarmed (the unit-bind cache, the vertex-bind memo, the sampler-unit memo, the alignment mirror — a surviving memo would SKIP re-asserts against the fresh context, the Task-75b regression class now on the loss path), the TF pass registry cleared (its stale ids degrade to the honest unknown-id no-ops). The cap probes (anisotropy/parallel-compile/float-linear) and the drawing-buffer notion stay — properties of the CONTEXT, not of the lost object population.

(2) webgl2Renderer's onContextRestored — the session path (resources: createResourceJournal()): (a) the raw facade resets (same facade object, so every closure holding it keeps working); (b) session.restore() clears the session's own stable-to-raw mappings and REPLAYS the journal — every texture/view/target re-created with content, stable ids hold by construction; (c) executor.invalidate() (the new optional method on GLExecutor) walks the command set dropping programId/bufferIds (programs+vertex buffers are DERIVED state per the session's own doc — they re-create lazily from the command specs on the next draw) and RE-DIRTIES every uniform field — the subtle trap: the arena's value-compare suppressed uploads because the DEAD program already had the values, and the fresh program would never see them; (d) the loop resumes if the loss stopped a running one (resumeOnRestore captured at loss), the canvas state re-syncs, the report names the replay stats. THE HONEST BOUNDARIES, named in the reports: the plain path (no journal) gets the boundary report (nothing can replay dead textures — re-boot); the v1 journal decorator likewise; the TF tier's passes and the feeds' external buffers belong to their tiers (their stale ids degrade to unknown-id no-ops, never silent zombies). dispose() removes BOTH listeners — a disposed renderer cannot be resurrected; the loss report now distinguishes the two paths (the session path promises the replay, the plain path tells the re-boot truth).

(3) THE API-SURFACE GAP this work exposed: the dist bundle did NOT export createResourceJournal — the resources: option was BUNDLE-UNUSABLE (the factory lived only in @rune/core's export surface, which the browser bundle never re-exported). The umbrella package (@rune/gl index) now re-exports createResourceJournal + the ResourceJournal/RestoreReport/WorkingSet types; the dist carries them.

THE CONTAINER FINDING (documented in the gate's header and the docs): this Chrome+SwiftShader combo NEVER delivers webglcontextrestored after WEBGL_lose_context.restoreContext() — a raw browser probe with zero library code shows the context stuck at isContextLost()===true forever; on real hardware the event is spec-pinned and well-supported. The live gate (scripts/task168-restore.mjs) therefore drives both honest halves: LEG 1 — the synthetic event pair dispatched on a LIVE context (the events carry exactly the browser's contract, so the full wire runs for real: the journal replays with a real createTexture, the command re-creates its program, the loop resumes and DRAWS), LEG 2 — the REAL webglcontextlost delivery via loseContext() (the live zombie guard: drawing stops, the report lands).

TESTS: packages/gl/tests/task168.test.ts — 4 pins on a full raw-context mock with event capture: the complete in-place cycle (loss stops the loop with the replay promise; restore replays the journal — createTexture count 2; the program re-creates — createProgram count 2; the uniform RE-UPLOADS to the fresh program — uniform4fv count 2; the loop RESUMES and draws; the stable texture id survives the incarnation change), the plain-path boundary (the WITHOUT-the-journal report, no auto-resume, start() still refuses with the context-loss message), the dispose guard (both listeners removed — neither event reaches the dead renderer), and the never-running-restore (resources recovered, no auto-start, manual start works after). webgl2Renderer.test.ts's dispose pin updated honestly (TWO listeners now — the loss wire AND the restore wire).

Gates: 1755/1755 tests (was 1751, +4); typecheck 6 pre-existing (the particles/task142 baseline, untouched); lint 0 errors / 375 warnings (baseline); build → dist/rune.esm.js + min with the wire and the journal export; demo:smoke OK (vfx GPU health clean, astral alive); scripts/task168-restore.mjs ALL CELLS PASS; scripts/task167-syncpoint.mjs PASS (thresholds re-based honestly — the container variance spans 3-27 frames/6s on healthy builds, the live cells pin liveness, the cadence arithmetic is mock-pinned); task152-keepalive PASS (both listeners removed at dispose did not disturb the park/resurrect contract).

Cache-busts ?v=168: rune.esm.js imports in demo/vfx/main.js, demo/vfx/demos/gpuEmbers.js, demo/astral/main.js; main.js?v=168 in demo/vfx/index.html; docs/particles-optimization.md gains the Task-168 dossier section.
